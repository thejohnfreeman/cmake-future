# This package configuration file was generated by the
# `future_install_project` extension in `cmake-future`:
# https://github.com/thejohnfreeman/cmake-future

@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
foreach(package_name @dependencies@)
  # TODO: Pass along the rest of the options that were passed to
  # `future_add_dependency`.
  find_dependency("${package_name}" REQUIRED)
endforeach()

include(${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@-targets.cmake)
set(@PROJECT_NAME@_COMPONENTS @components@)
set(@PROJECT_NAME@_TARGETS @targets@)

foreach(component @components@)
  set(@PROJECT_NAME@_${component}_FOUND TRUE)
endforeach()

# This function sets `@PROJECT_NAME@_FOUND`.
check_required_components(@PROJECT_NAME@)

message(STATUS "Found: @PROJECT_VERSION@ @PROJECT_NAME@")
