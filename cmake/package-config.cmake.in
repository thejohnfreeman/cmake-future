if(@package_name@_FOUND)
  return()
endif(@package_name@_FOUND)

macro(find_extension name)
  find_package(${name} @PROJECT_VERSION@ EXACT REQUIRED)
endmacro()

@text@

message(STATUS "Found: @PROJECT_VERSION@ @package_name@")
